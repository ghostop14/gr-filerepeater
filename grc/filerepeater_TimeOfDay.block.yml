# auto-generated by grc.converter

id: filerepeater_TimeOfDay
label: Time Of Day
category: '[Advanced File]'

parameters:
-   id: hours
    label: Time Hour (24h)
    dtype: int
    default: '15'
-   id: minutes
    label: Time Minute
    dtype: int
    default: '00'
-   id: seconds
    label: Time Seconds
    dtype: int
    default: '00'
-   id: triggerTime
    label: Active Time (s)
    dtype: float
    default: '300.0'

outputs:
-   domain: message
    id: trigger
    optional: true

templates:
    imports: import filerepeater
    make: filerepeater.TimeOfDay(${hours}, ${minutes}, ${seconds}, ${triggerTime})

documentation: |-
    This block generates state messages.  A state=1 is sent at the specified time of day, then a state=0 is sent when the specified length expires.  The message block contains a "state" meta dictionary entry with 1 for on, 0 for off.

file_format: 1
