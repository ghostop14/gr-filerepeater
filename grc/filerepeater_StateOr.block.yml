# auto-generated by grc.converter

id: filerepeater_StateOr
label: State Message Or
category: '[Advanced File]'

inputs:
-   domain: message
    id: state1
-   domain: message
    id: state2

outputs:
-   domain: message
    id: state

templates:
    imports: import filerepeater
    make: filerepeater.StateMessageOr()

documentation: |-
    This block monitors the two inbound state message ports looking for the metadata 'state' flag (1=True, 0=False).  The block then does a boolean OR and will generate a single message based on boolean OR.

file_format: 1
