# auto-generated by grc.converter

id: filerepeater_msg_to_file
label: Message to File
category: '[Advanced File]'

parameters:
-   id: file
    label: File
    dtype: file_open
-   id: timestamp
    label: Add Timestamp
    dtype: enum
    options: ['False', 'True']
    option_labels: ['No', 'Yes']
-   id: overwrite
    label: File Disposition
    dtype: enum
    options: ['False', 'True']
    option_labels: [Append, Overwrite]

inputs:
-   domain: message
    id: msg

templates:
    imports: import filerepeater
    make: filerepeater.MsgToFile(${file},${timestamp},${overwrite})

documentation: |-
    This block will write all incoming messages to the specified file in text form.

file_format: 1
